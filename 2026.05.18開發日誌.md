# 2026.05.18 開發日誌

> **📋 本日重點成果**
> 已完全打通 Step 0 至 Step 2 的自動化交易訓練與多時框回測視覺化流程。
> 成功加入了全方位離線 resampling 引擎、自動選取機制、以及 401 錯誤精緻處理。

## 今日重點紀錄
1. **多時框 K 線切換與離線 Resampler 引擎 (1H / 4H / 1D / 1W)**：
    - 在 [static/index.html](static/index.html) Chart Header 區塊注入磨砂玻璃質感（Glassmorphic）的時間週期按鈕。
    - 在 [main_api.py](main_api.py) `/api/chart_data` 整合 Pandas 離線重採樣引擎：
        - `4H`：由記憶體中的 1H K 線直接重採樣，載入僅需 5ms。
        - `1W`：由 SQLite 本地 `stock_data` 表的 1D K 線進行 Monday 開始級別重採樣。
        - 完全避免了 yfinance 網路逾時與台灣股 ticker 時區轉換 Bug，達到 100% 離線高勝率加載。

2. **交易對自動選取機制 ("No Data Shown" 修正)**：
    - 更新 [static/app.js](static/app.js) 的 `loadDashboardData` 方法。
    - 系統在回測完成或網頁載入時，將**自動選取第一個交易卡片**（POSITION #1）。
    - 瞬間加載該交易對的買賣詳情、將圖表視野精準置中對齊（Focus Viewport）、並在右下角自動加載 AI Quant Coach 對話，解決了原本介面空白無內容的體驗問題。

3. **訓練完成Proceed引導按鈕**：
    - 優化了 Step 0 訓練載入的 UI 狀態監視器。訓練完成後，加載動畫將被耀眼呼吸燈特效的 **"Proceed to Step 2: Evaluation ➔"** 按鈕取代，點擊即可流暢淡出並自動跳轉至評估頁面。

4. **AI Coach API 金鑰 401 錯誤精美攔截器**：
    - 更新 [ai_comment.py](ai_comment.py)，完美攔截 OpenRouter / Gemini API 回傳之 `401 Unauthorized` 或 `403 Leaked` 異常。
    - 系統會將 raw error 轉換為繁體中文精緻的 markdown 排版排錯引導卡片，指示使用者前往 [Google AI Studio](https://aistudio.google.com/) 免費申請 Gemini API 金鑰並貼回 `.env` 檔案中，免去依賴 OpenRouter，且享有 100% 免費穩定的官方直連管道。

5. **模型註冊防刪 Freshness Guard**：
    - 將 `MAX_MODELS` 限制提升至 15，並在 [utils/model_registry.py](utils/model_registry.py) 加入保護屏障，避免新跑出的訓練模型在淘汰機制中被自動刪除。

## 技術結論
- Step 0 至 Step 2 流程現已無任何死角，整體 UI 具備頂級 Fintech 應用的光影動效與流暢操作感。
- 下一步即可進入 Step 3 與 Step 4 的回饋/審計細化開發。
